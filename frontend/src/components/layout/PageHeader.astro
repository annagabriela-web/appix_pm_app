---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<header class="mb-6">
  <h2 class="text-2xl font-bold text-gray-900">{title}</h2>
  {subtitle && <p class="text-sm text-neutral mt-1">{subtitle}</p>}
  <slot />
</header>
