TECHSTACK.md - The Engineering Contract1. Core StackThis stack is chosen to support high-fidelity data visualization on the frontend and robust data processing on the backend.FrontendMeta-Framework: Astro 4.0 (for high-performance shell and static content).UI Library: React 18+ (for all interactive islands).Visualization: Recharts (MANDATORY per requirements).Styling: Tailwind CSS (Utility-first, strictly bound to Design Tokens).State Management: TanStack Query (React Query) for API synchronization.BackendLanguage: Python 3.11+.Framework: Django 5.0 (Chosen for robust ORM, Admin panel for Rates management, and stability).Async Tasks: Celery + Redis (MANDATORY for Clockify/Jira syncing).PDF Generation: ReportLab or WeasyPrint.Excel: OpenPyXL.DatabasePrimary: PostgreSQL 16.Caching: Redis (for Celery broker and potential API caching).2. External IntegrationsClockify API:Endpoints: /workspaces/{id}/projects, /time-entries.Auth: API Key.Jira API:Existing integration (assumed available).3. Configuration & ConstraintsStrict Mode: TypeScript strict: true is enabled. No any.Currency Math: Backend must use Decimal type. Frontend should display formatted strings, never perform float math on money.Sync Frequency: 1 Hour (Automated via Celery Beat).4. Forbidden TechnologiesNo Client-Side Only Data Processing: Heavy aggregation of "Hours per Phase" must happen in Python/SQL, not in the Browser.No jQuery: Use standard DOM APIs if needed outside React.No Bootstrap: Use Tailwind.